# JetBrains
/.idea/
*.iml

# Validation output (task 6) — regenerated, never reviewed by hand
/out/
*.gcode.gz
scripts/**/tmp/

# Python
__pycache__/
*.pyc

# OS / editor cruft
.DS_Store
*~
*.swp

# OrcaSlicer CLI debris (fixed name, written to the working directory)
/00000.log

# QIDI's published profile bundle: downloaded, never committed.
# See reference/qidi-profiles.md.
/reference/qidi-profiles/

# release workflow output
/dist/

# Print evidence: photos off the machine and G-code exported for a test print.
# Working files, never committed -- this repo carries profiles and the
# documentation of where their values came from, not the prints they produced.
# TODO.md and intent/0004 name the ones worth keeping; they live beside the repo.
/Photos-1-001/
/*.gcode
